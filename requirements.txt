# python version should be <= 3.12.7 or older
# Requirements for the Flask web application
Flask==2.3.2
Flask-CORS==4.0.0

# Requirements for automated object generation
watchdog 
requests

# Requirements for file sync
paramiko

# Requirements for raspberry pi script
pygame
opencv-python>=4.8.0
pyserial

# Requirements for trashcam (main machine)
pynput

# Requirements for SAM2 segmentation
torch>=2.0.0
torchvision>=0.15.0
sam2>=1.0
numpy
pillow